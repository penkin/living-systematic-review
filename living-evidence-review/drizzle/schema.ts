import { int, mysqlEnum, mysqlTable, text, timestamp, varchar } from "drizzle-orm/mysql-core";

export const users = mysqlTable("users", {
  id: int("id").autoincrement().primaryKey(),
  openId: varchar("openId", { length: 64 }).notNull().unique(),
  name: text("name"),
  email: varchar("email", { length: 320 }),
  loginMethod: varchar("loginMethod", { length: 64 }),
  role: mysqlEnum("role", ["user", "admin"]).default("user").notNull(),
  createdAt: timestamp("createdAt").defaultNow().notNull(),
  updatedAt: timestamp("updatedAt").defaultNow().onUpdateNow().notNull(),
  lastSignedIn: timestamp("lastSignedIn").defaultNow().notNull(),
});

export const reviewRecords = mysqlTable("review_records", {
  id: varchar("id", { length: 32 }).primaryKey(),
  payload: text("payload").notNull(),
  reviewerDecision: varchar("reviewerDecision", { length: 32 }),
  reviewerReason: text("reviewerReason"),
  reviewerInitials: varchar("reviewerInitials", { length: 32 }),
  reviewedAt: timestamp("reviewedAt"),
  createdAt: timestamp("createdAt").defaultNow().notNull(),
  updatedAt: timestamp("updatedAt").defaultNow().onUpdateNow().notNull(),
});

export type User = typeof users.$inferSelect;
export type InsertUser = typeof users.$inferInsert;
export type ReviewRecordRow = typeof reviewRecords.$inferSelect;
export type InsertReviewRecord = typeof reviewRecords.$inferInsert;
